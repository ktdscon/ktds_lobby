# 카카오톡 아침 알림 설정 (30분 정도 걸립니다)

매일 아침 카카오톡 **"나와의 채팅"** 으로 그날 할 일을 받는 기능입니다.

```
☀️ 9월 14일 (월)

📌 오늘 할 일
· 사내 대부 대출 신청 — 인사포털에서 신청
· 주간보고 제출 (17:00)

⏳ 다가오는 일
· 자격증 접수 마감 — D-3 (9/17)
```

## 왜 이 구조인가

Claude(AI)는 대화가 끝나면 사라지기 때문에 매일 아침 혼자 깨어나 카톡을 보낼 수 없습니다.
그래서 **24시간 켜져 있는 발송기**가 필요한데, 이 프로젝트가 이미 쓰고 있는
**구글시트 + Apps Script**를 그대로 씁니다. 무료고, 서버 관리가 없고, 구글 계정만 있으면 됩니다.

- 할 일 목록 → 구글시트 `Reminders` 탭 (폰의 구글 스프레드시트 앱에서도 편집 가능)
- 발송 → Apps Script의 매일 아침 타이머 트리거
- 카카오 → 카카오 메시지 API의 **"나에게 보내기"**
  (본인에게 보내는 것은 앱 심사·사업자등록 없이 개인 개발자 앱으로 바로 가능합니다.
  친구/단체 발송은 심사가 필요하지만 여기서는 쓰지 않습니다.)

---

## 1단계 — 카카오 개발자 앱 만들기 (약 10분)

1. https://developers.kakao.com 에 **카카오톡을 쓰는 그 계정으로** 로그인
   (⚠️ 알림을 받을 카카오톡 계정과 반드시 같은 계정이어야 합니다)
2. **내 애플리케이션 > 애플리케이션 추가하기**
   - 앱 이름: `내 아침 알림` (자유)
   - 사업자명: 개인이면 본인 이름
3. 만들어진 앱에 들어가서 **앱 키** 메뉴 → **REST API 키** 를 복사해 둡니다 (나중에 씁니다)
4. **플랫폼 > Web** → 사이트 도메인에 `https://localhost` 추가 후 저장
5. **카카오 로그인** 메뉴 → **활성화 설정: ON**
6. 같은 화면 아래 **Redirect URI 등록** → `https://localhost` 추가 후 저장
   (이 주소는 실제로 열리지 않아도 됩니다. 주소창에 붙는 코드만 쓰거든요)
7. **카카오 로그인 > 동의항목** → **카카오톡 메시지 전송 (`talk_message`)** 찾아서
   **"선택 동의"** 로 설정 + 동의 목적에 `내 일정 알림 발송` 같이 적고 저장

## 2단계 — Apps Script에 코드 넣기 (약 5분)

1. 로비 안내 화면이 쓰는 그 **구글시트**를 엽니다
   (일정 데이터가 들어있는 그 시트. 없으면 새로 만들어도 됩니다)
2. **확장 프로그램 > Apps Script**
3. 왼쪽 파일 목록에서 **+ > 스크립트**를 눌러 새 파일을 만들고 이름을 `KakaoReminder` 로 지정
4. 이 저장소의 **`KakaoReminder.gs` 내용을 전부 복사해서 붙여넣고 저장** (Ctrl+S)
5. `AppsScript_Code.gs`도 이 저장소 최신 내용으로 갱신해 주세요
   (`doGet`에 알림용 action을 넘기는 3줄이 추가됐습니다. `SHARED_KEY`는 Apps Script 안에서만
   실제 값으로 바꿔 쓰던 그 값을 유지하세요 — 저장소에는 플레이스홀더만 들어있습니다)
6. 왼쪽 **⚙️ 프로젝트 설정** → **시간대가 `(GMT+09:00) 서울`** 인지 확인 (아니면 바꿔주세요)

## 3단계 — 카카오 연결 (약 10분, 한 번만)

Apps Script 편집기 위쪽의 **함수 선택 드롭다운**에서 함수를 골라 **실행 ▶** 하는 방식입니다.
실행 결과는 아래 **실행 로그**에 뜹니다. 처음 실행할 때 구글 권한 승인 창이 뜨는데,
"고급 > 안전하지 않은 페이지로 이동"을 눌러 본인 계정에 권한을 허용해 주세요.

1. **REST API 키 저장** — 편집기 코드 아무 곳에서나 아래 한 줄을 임시로 만들어 실행하거나,
   더 쉽게는 **왼쪽 ⚙️ 프로젝트 설정 > 스크립트 속성 > 속성 추가** 에서 직접 넣어도 됩니다:
   - 속성 이름: `KAKAO_REST_KEY`
   - 값: 1단계에서 복사한 REST API 키
2. **`kakaoAuthUrl`** 함수 실행 → 실행 로그에 긴 URL이 찍힙니다. 그 URL을 복사해 브라우저에서 열고
   **"동의하고 계속하기"** 를 누릅니다.
3. 화면은 "사이트에 연결할 수 없음"처럼 보이는 게 정상입니다.
   **주소창**을 보면 `https://localhost/?code=XXXXXXXXXX` 형태입니다. `code=` 뒤의 값만 복사합니다.
4. Apps Script로 돌아와 코드 맨 아래에 임시로 이 줄을 추가하고 그 함수를 실행합니다:
   ```javascript
   function 한번만실행() { kakaoExchangeCode('여기에_복사한_코드_붙여넣기'); }
   ```
   로그에 "토큰 저장 완료"가 나오면 성공입니다. (이 임시 함수는 지워도 됩니다.
   ⚠️ **코드 값이나 토큰을 저장소에 커밋하지 마세요.** 전부 스크립트 속성에만 보관됩니다)
   - 인가코드는 **1회용이고 몇 분 만에 만료**됩니다. 실패하면 2번부터 다시 하면 됩니다.
5. **`sendTestKakao`** 함수 실행 → 카카오톡 "나와의 채팅"에 테스트 메시지가 오는지 확인
6. **`installDailyTrigger`** 함수 실행 → 매일 아침 알림 켜기 (기본 8시)

끝났습니다. 이제 시트에 할 일만 적으면 됩니다.

---

## 할 일 적는 법 — 구글시트 `Reminders` 탭

`Reminders` 탭은 처음 만들어질 때 **NOL티켓 예매 내역(2026-09-12 기준)이 미리 채워져** 있습니다.
바로 쓰시면 되고, 필요 없는 줄은 지우면 됩니다.

| 날짜 | 할 일 | 시간 | 미리알림(일) | 반복 | 완료 | 메모 |
|---|---|---|---|---|---|---|
| 2026-09-13 | 와일드스미스 그림책 원화展 — 오늘이 마지막 날 | | | | | 예술의전당 서예박물관 / T2983174670 |
| 2026-09-19 | 웨인 티보 전 관람 시작 (~12/18) | | | | | DDP 뮤지엄 / T3019478430 |
| 2026-10-24 | 이자람 판소리 '눈, 눈, 눈' 관람 | 16:00 | 7,3,1 | | | LG아트센터 서울 / 3314647816 |
| 2026-12-18 | 웨인 티보 전 관람 마감 | | 30,14,7,1 | | | DDP 뮤지엄 / T3019478430 |
| 2026-09-14 | 사내 대부 대출 신청 | | 3,1 | | | 인사포털에서 신청 |
| | (예시) 매주 금요일 반복 항목은 이렇게 | 17:00 | | 매주 금 | ✔ | 완료 칸을 비우면 그때부터 알림이 옵니다 |

기간이 정해진 전시는 **시작일 1줄 + 마감일 1줄**로 넣는 걸 권합니다.
"아직 시간 많네" 하다가 놓치는 게 보통 기간 전시라, 마감일 줄에 `30,14,7,1` 같은 미리알림을 걸어두면
D-30부터 네 번 찔러줍니다.

- **날짜**: `2026-09-14` 형식. `2026.9.14`, `2026/9/14` 도 알아서 인식합니다.
  반복 항목은 날짜를 **비워두세요**.
- **할 일**: 카톡에 그대로 나오는 문구. 이것만 있으면 동작합니다.
- **시간**: 표시용 (예 `10:00`). 이 시간에 따로 알림이 오는 건 아닙니다.
- **미리알림(일)**: `3,1` 이면 3일 전과 1일 전 아침에도 "D-3", "D-1"로 알려줍니다.
  비워두면 그날 아침에만 옵니다. (날짜가 정해진 항목에만 적용)
- **반복**: `매일` / `평일` / `주말` / `매주 금` / `매주 월,수` / `매월 25` / `매월 말` / `매년 09-15`
  - `매월 31` 처럼 그 달에 없는 날짜는 그 달 **마지막 날**에 알려줍니다.
- **완료**: `✔`, `O`, `v`, `완료`, `done` 등 **아무 표시나** 넣으면 그 항목은 더 이상 알림이 오지 않습니다.
  (`x`, `-`, `0`, `미완료`, `아직`, `진행중`은 "아직 안 끝남"으로 봅니다)
  끝난 일은 지워도 되고 완료 표시만 해둬도 됩니다.
- **메모**: 카톡 메시지에 `— 메모` 형태로 덧붙습니다.

행 순서는 상관없습니다. 아무 데나 추가하면 됩니다.

## 주간 보고 (매주 월요일 아침)

매일 아침 알림과 **별개로** 켜고 끕니다. `installWeeklyTrigger` 함수를 한 번 실행하면 끝입니다.

```
📋 주간 보고 (9/14~9/20)

🔴 밀린 일 1건
· 와일드스미스 그림책 원화展 (9/13 지남)

📌 이번 주 할 일 2건
· 9/14(월) 사내 대부 대출 신청
· 9/19(토) 웨인 티보 전 관람 시작 (~12/18)

📚 강의장 운영
· 이번 주 4건 / 다음 주 1건
· ⚠️ 평일인데 일정 없음: 9/17(목), 9/18(금)
· ⚠️ "(테스트)" 임시 데이터 남아있음: 9/22
```

- **밀린 일** = 날짜가 지났는데 완료 표시가 없는 항목. 매일 알림에는 안 나오고 주간 보고에만 나옵니다
- **강의장 운영** 항목은 `setIncludeLobbySchedule(true)` 를 켠 경우에만 붙습니다.
  평일인데 강의가 하나도 없는 날과, 아직 `(테스트)` 임시 데이터가 남아있는 날을 잡아줍니다
  (이번 주 + 다음 주 2주치를 보므로 미리 고칠 시간이 있습니다)
- 요일·시각을 바꾸려면 `setWeeklySchedule('금', 17)` 처럼 실행하세요 (매주 금요일 5시)

## 바깥에서 카톡으로 글 밀어넣기 (Claude Code 주간 보고 등)

Apps Script 웹앱에 POST를 보내면 **아무 텍스트나** 내 카카오톡으로 갑니다.
Claude Code가 만든 주간 보고, 다른 스크립트의 알림 등을 여기로 보내면 됩니다.

```bash
curl -L -X POST '<웹앱URL>' \
  -H 'Content-Type: application/json' \
  -d '{"key":"<SHARED_KEY>","action":"sendKakao","text":"이번 주 작업 요약\n· ...\n· ..."}'
```

```bash
# 시트 기반 주간 보고를 지금 당장 받기
curl -L -X POST '<웹앱URL>' -H 'Content-Type: application/json' \
  -d '{"key":"<SHARED_KEY>","action":"weeklyNow"}'
```

`-L` 은 필수입니다 (Apps Script 웹앱이 302로 리디렉션합니다).
200자가 넘으면 자동으로 나눠서 갑니다.

⚠️ 이 요청에는 `SHARED_KEY` 가 들어갑니다. **저장소나 공개된 곳에 적지 마세요.**

## 자주 쓰는 함수

| 함수 | 하는 일 |
|---|---|
| `previewBriefing` | **카톡을 보내지 않고** 오늘 어떤 메시지가 갈지 로그로만 확인 |
| `previewTomorrow` | 내일 갈 메시지 미리보기 |
| `sendDailyBriefing` | 지금 당장 오늘 브리핑 발송 (트리거가 매일 실행하는 함수) |
| `sendTestKakao` | 연결 확인용 테스트 메시지 발송 |
| `setBriefHour(7)` | 발송 시각 변경 (0~23). 실행 후 `installDailyTrigger` 를 **다시** 실행해야 적용 |
| `setIncludeLobbySchedule(true)` | 아침 메시지에 그날 강의장 일정(`Schedule` 탭)도 함께 넣기 |
| `setBriefLink('https://...')` | 카톡 메시지 아래 버튼이 열 주소 변경 (기본: 로비 안내 화면) |
| `installDailyTrigger` / `removeDailyTrigger` | 매일 아침 알림 켜기 / 끄기 |
| `previewWeeklyReport` | **보내지 않고** 이번 주 주간 보고 내용만 확인 |
| `sendWeeklyReport` | 지금 당장 주간 보고 발송 |
| `installWeeklyTrigger` / `removeWeeklyTrigger` | 주간 보고 켜기 / 끄기 (기본 월요일 아침) |
| `setWeeklySchedule('금', 17)` | 주간 보고 요일·시각 변경 |

## 폰에서 바로 할 일 추가하기 (선택)

Apps Script 웹앱이 배포되어 있으면 주소창 한 번으로 할 일을 추가할 수 있습니다.
아이폰 "단축어"나 안드로이드 위젯에 이 URL을 등록해두면 편합니다.

```
<웹앱URL>?action=addReminder&key=<SHARED_KEY>&title=대출신청&date=2026-09-14&before=3,1
<웹앱URL>?action=addReminder&key=<SHARED_KEY>&title=주간보고&repeat=매주 금&time=17:00
<웹앱URL>?action=briefNow&key=<SHARED_KEY>          ← 지금 당장 카톡으로 받기
```

`<SHARED_KEY>` 는 로비 백엔드에서 쓰는 그 키입니다 (SECRETS.local.md 참고).
이 URL에는 키가 들어있으니 **남에게 공유하지 마세요.**

## 알아둘 점 / 문제 해결

- **발송 시각은 ±1시간 오차가 있습니다.** 구글 타이머 트리거는 "8시 정각"이 아니라
  "8시~9시 사이"에 실행됩니다. 정확한 분 단위가 필요하면 구조를 바꿔야 합니다.
- **보낼 내용이 없는 날은 카톡이 오지 않습니다.** (빈 메시지로 귀찮게 하지 않으려고요)
  매일 무조건 받고 싶으면 `Reminders` 탭에 반복 `매일` 짜리 항목 하나를 넣어두면 됩니다.
- **카카오 메시지는 200자 제한**이 있어서, 할 일이 많으면 자동으로 `(1/2)`, `(2/2)` 로 나눠 옵니다.
- **토큰 만료**: 카카오 refresh token은 60일이지만 매일 발송하면서 자동 갱신되므로 사실상 무한입니다.
  단, 두 달 넘게 발송이 멈춰 있었다면 3단계(`kakaoAuthUrl` → `kakaoExchangeCode`)를 다시 하세요.
- **며칠째 카톡이 안 온다면**: Apps Script 편집기 왼쪽 **실행** 메뉴에서 `sendDailyBriefing`의
  실패 로그를 확인하세요. 오류가 반복되면 구글이 트리거를 자동으로 끄고 계정 메일로 알려줍니다.
  고친 뒤 `installDailyTrigger` 를 다시 실행하면 됩니다.
- **`unauthorized` 응답**: URL의 `key=` 값이 Apps Script의 `SHARED_KEY`와 다릅니다.
- **`KOE320` / `invalid grant`**: 인가코드가 이미 쓰였거나 만료됐습니다. `kakaoAuthUrl`부터 다시.
- **`insufficient scope`**: 1단계 7번의 `talk_message` 동의항목이 꺼져 있습니다. 켜고 3단계를 다시.
