# KT ds 교육센터 로비 안내 화면 - 작업 이어가기

D:\컨소시엄\20260815_lobby 폴더에서 이어서 작업. 프로젝트 전반 규칙은 [CLAUDE.md](CLAUDE.md) 참고 — 이 파일은 "무엇을 왜 했는지"의 서술형 기록.

## 파일 구조

- **index.html**: 실제 서비스 파일 (단일 파일, 강의장 위치 이미지도 base64로 파일 안에 내장되어 있음)
- **_artifact_preview.html**: claude.ai 아티팩트 게시용 보기 전용 사본. index.html과 항상 수동으로 동기화 필요
- **AppsScript_Code.gs**: Google Sheet 연동용 백엔드 코드 템플릿. `SHARED_KEY`는 플레이스홀더만 커밋, 실제 값은 Apps Script 편집기에만 존재
- **assets/floor_plan_2f.png**: 강의장 위치 원본 이미지 (index.html 안에도 내장돼 있어서 이 파일 자체는 백업용)
- **SECRETS.local.md**: 실제 배포 URL/키 (git에는 안 올라감, 로컬 전용)
- **.gitignore**: SECRETS.local.md와 스크래치 파일 제외

## 화면 구성

- 왼쪽: 오늘의 교육과정 리스트 — 201호~212호가 항상 다 보이고, 강의 없으면 공란
- 오른쪽: 강의장 위치 — 실제 사진(2F 강의장 안내 이미지)을 배경으로 깔고, 그 위에 투명 오버레이로 클릭/하이라이트/경로선(현재위치→로비→강의실) 구현
- 헤더: 시계/날짜, 로고
- 하단: 구역 필터(R/G/B)·검색, NOTICE 롤링 문구
- 우하단 톱니바퀴(PIN 기본값 1234) → 관리자 모드: 설정/동기화, 붙여넣기, 검토&게시, 일정 수정 4탭

## 이번 세션에서 한 작업 (2026-08-16)

### 1. CSS 레이아웃 버그 수정
- **그리드 오버플로우 버그**: `#main{grid-template-columns:1fr 1fr}`에 `minmax(0,1fr)`이 없고 `#listPanel/#mapPanel`에 `min-width:0`이 없어서, 창 폭이 좁을 때(작은 미리보기 등) 그리드가 내용물 크기만큼 넘쳐 지도가 잘리는 문제가 있었음 → 수정
- **작은 미리보기 축소 표시**: `#app`을 고정 1920×1080 박스로 만들고 `fitAppScale()`(창 크기에 맞춰 `transform:scale()`)을 추가. 1920×1080 실배포에서는 scale=1이라 기존과 완전히 동일하고, 좁은 창에서만 전체 화면 비율을 유지한 채 축소됨
- `.logo`에 `white-space:nowrap;flex-shrink:0;` 방어적 추가 (제목 줄바꿈 방지)
- **리스트 줄 높이 캡 버그**: `syncRowHeight()`가 줄 높이를 최대 60px로 캡해놔서, 지도 패널은 꽉 차는데 왼쪽 리스트만 아래에 빈 공간이 남는 문제가 있었음 → 캡 제거, 12개 방이 패널 높이를 정확히 채우도록 수정

### 2. 콘텐츠/UX
- NOTICE 롤링 문구 기본값에 "인재육성팀 8F 안내", "출결 APP 가이드(https://ktdscon.github.io/int/)" 추가
- 붙여넣기 탭 UX 단순화: "① 복사 → ② 붙여넣기 → ③ 끝!" 3단계 안내, 버튼명 "붙여넣은 일정 반영하기"로 변경, 요일까지 전부 자동 인식되면(실제 HRCMS 데이터는 대부분 이 케이스) 검토 화면 없이 바로 게시하고 초록 배너로 완료 표시
- 일정 수정 탭 버튼 "수정 반영하기"로 통일, **개별 항목의 날짜를 직접 바꿔서 다른 날짜로 이동**시키는 기능 추가 (원래 날짜에서 삭제 + 새 날짜의 기존 일정에 병합). 통째로 다시 붙여넣지 않아도 오타/날짜/시간 하나만 고칠 수 있음
- 같은 강의실에 주간/야간 두 과정이 있을 때, 야간(또는 명시적 시간대)만 배지로 표시하고 주간은 배지 없이 표시 (보통 주간이 디폴트라는 사용자 피드백 반영)
- **자정 자동 전환**: 그동안 `currentDate`가 페이지 로드 시 한 번만 설정되고 자정이 지나도 갱신되지 않는 버그가 있었음(24시간 켜져 있는 키오스크라 심각한 문제였음) → `tickClock()`에 day-rollover 감지 추가. 화면이 "오늘"을 보고 있던 경우에만 자동으로 다음 날로 넘어가고, 관리자가 일부러 다른 날짜를 보고 있었다면 그대로 유지됨

### 3. 배포
- **GitHub Pages**: `ktdscon` 계정으로 `gh` CLI 설치·인증 후 `ktds_lobby` 공개 저장소 생성, Pages 활성화 → https://ktdscon.github.io/ktds_lobby/ 에서 정상 서비스 중
- **Google Apps Script**: 사용자가 직접 구글 시트 생성 + Apps Script 배포 (프로젝트명 "2026_lobby"), `?action=ping` 확인 완료

### 4. 보안 사고 및 조치 (중요)
편의를 위해 실제 배포된 Apps Script URL과 `SHARED_KEY`(`ktds-lobby-2026`)를 index.html의 기본 설정값에 하드코딩했었는데, 이 저장소가 **공개(Public)**라서 그대로 노출되는 실수를 저질렀음.
- 즉시 되돌림: 기본값을 다시 빈 값으로 복원 (`apiUrl:'', apiKey:''`)
- `AppsScript_Code.gs`의 `SHARED_KEY` 예시값도 `CHANGE_ME_BEFORE_DEPLOY` 플레이스홀더로 변경
- 사용자에게 실제 Apps Script의 `SHARED_KEY`를 새 값으로 교체하고 같은 배포에 "새 버전"으로 재배포(URL 유지)하도록 안내함
- **주의**: git 커밋 히스토리에는 예전 키 값이 과거 커밋으로 남아있을 수 있음(rebase로 지우진 않음, 이미 무효화된 값이라 실익 없다고 판단) — 새 키를 SECRETS.local.md에만 적어두고, 절대 소스코드나 커밋 메시지에 다시 남기지 말 것

## 확인된 사항 / 남은 일

- 강의장 배치는 (리뉴얼) 최신 도면 + 실사 이미지 기준으로 정확히 맞춤 (201-212호, 사무실, 로비, 엘리베이터, 화장실, 입구 위치 포함)
- `[211] 28기 채용예정자과정`은 원본 데이터에 일요일 칸에만 찍혀 있어 평일 상시 진행으로 가정 처리함 — 정확한 요일 확인 필요
- **남은 일**:
  1. 실제 Apps Script `SHARED_KEY` 교체 확인 (위 보안 사고 조치 완료 여부)
  2. 새 키로 "설정 공유 링크" 만들어서 팀원 노트북에 1회씩 배포 (SECRETS.local.md에 방법 적어둠)
  3. 8~12월 스케줄 데이터를 아직 구글시트에 붙여넣지 않은 상태 — 관리자 모드 붙여넣기 탭에서 월별로 반복 진행 필요
  4. 로비 PC에서 전체화면(F11)으로 실사용 테스트
  5. 사용자가 2026-08-18 이후 다른 유료 Claude 계정으로 작업을 이어갈 예정 — 새 세션은 이 폴더를 열면 CLAUDE.md가 자동으로 로드되니 그대로 이어가면 되고, 막히면 이 문서를 먼저 읽을 것
