# KT ds 교육센터 로비 안내 화면 - 작업 이어가기

D:\컨소시엄\20260815_lobby 폴더에서 이어서 작업. 프로젝트 전반 규칙은 [CLAUDE.md](CLAUDE.md) 참고 — 이 파일은 "무엇을 왜 했는지"의 서술형 기록.

## 파일 구조

- **index.html**: 실제 서비스 파일 (단일 파일, 강의장 위치 이미지도 base64로 파일 안에 내장되어 있음)
- **_artifact_preview.html**: claude.ai 아티팩트 게시용 보기 전용 사본. index.html과 항상 수동으로 동기화 필요
- **AppsScript_Code.gs**: Google Sheet 연동용 백엔드 코드 템플릿. `SHARED_KEY`는 플레이스홀더만 커밋, 실제 값은 Apps Script 편집기에만 존재
- **KakaoReminder.gs**: 매일 아침 카카오톡으로 그날 할 일을 보내주는 알림 발송기 (같은 Apps Script 프로젝트에 나란히 두는 파일). 로비 화면과 독립
- **KAKAO_REMINDER_설정.md**: 위 알림 기능 설정 절차 + 시트 작성법
- **assets/floor_plan_2f.png**: 강의장 위치 원본 이미지 (index.html 안에도 내장돼 있어서 이 파일 자체는 백업용)
- **SECRETS.local.md**: 실제 배포 URL/키 (git에는 안 올라감, 로컬 전용)
- **.gitignore**: SECRETS.local.md와 스크래치 파일 제외

## 화면 구성

- 왼쪽: 오늘의 교육과정 리스트 — 201호~212호가 항상 다 보이고, 강의 없으면 공란
- 오른쪽: 강의장 위치 — 실제 사진(2F 강의장 안내 이미지)을 배경으로 깔고, 그 위에 투명 오버레이로 클릭/하이라이트/경로선(현재위치→로비→강의실) 구현
- 헤더: 시계/날짜, 로고
- 하단: 구역 필터(R/G/B)·검색, NOTICE 롤링 문구
- 우하단 톱니바퀴(PIN 기본값 1234) → 관리자 모드: 설정/동기화, 붙여넣기, 검토&게시, 일정 수정, 웰컴 영상 5탭
- 손님 방문 등 특별한 경우, "웰컴 영상" 탭에서 설정한 기간 동안 전체화면 영상이 자동 재생되고 기간이 지나면(또는 화면 터치 시) 자동으로 원래 교육일정 화면으로 돌아감

## 이번 세션에서 한 작업 (2026-08-16)

### 1. CSS 레이아웃 버그 수정
- **그리드 오버플로우 버그**: `#main{grid-template-columns:1fr 1fr}`에 `minmax(0,1fr)`이 없고 `#listPanel/#mapPanel`에 `min-width:0`이 없어서, 창 폭이 좁을 때(작은 미리보기 등) 그리드가 내용물 크기만큼 넘쳐 지도가 잘리는 문제가 있었음 → 수정
- **작은 미리보기 축소 표시**: `#app`을 고정 1920×1080 박스로 만들고 `fitAppScale()`(창 크기에 맞춰 `transform:scale()`)을 추가. 1920×1080 실배포에서는 scale=1이라 기존과 완전히 동일하고, 좁은 창에서만 전체 화면 비율을 유지한 채 축소됨
- `.logo`에 `white-space:nowrap;flex-shrink:0;` 방어적 추가 (제목 줄바꿈 방지)
- **리스트 줄 높이 캡 버그**: `syncRowHeight()`가 줄 높이를 최대 60px로 캡해놔서, 지도 패널은 꽉 차는데 왼쪽 리스트만 아래에 빈 공간이 남는 문제가 있었음 → 캡 제거, 12개 방이 패널 높이를 정확히 채우도록 수정

### 2. 콘텐츠/UX
- NOTICE 롤링 문구 기본값에 "인재육성팀 8F 안내", "출결 APP 가이드(https://ktdscon.github.io/int/)" 추가
- 붙여넣기 탭 UX 단순화: "① 복사 → ② 붙여넣기 → ③ 끝!" 3단계 안내, 버튼명 "붙여넣은 일정 반영하기"로 변경, 요일까지 전부 자동 인식되면(실제 HRCMS 데이터는 대부분 이 케이스) 검토 화면 없이 바로 게시하고 초록 배너로 완료 표시
- 일정 수정 탭 버튼 "수정 반영하기"로 통일, **개별 항목의 날짜를 직접 바꿔서 다른 날짜로 이동**시키는 기능 추가 (원래 날짜에서 삭제 + 새 날짜의 기존 일정에 병합). 통째로 다시 붙여넣지 않아도 오타/날짜/시간 하나만 고칠 수 있음
- 같은 강의실에 주간/야간 두 과정이 있을 때, 야간(또는 명시적 시간대)만 배지로 표시하고 주간은 배지 없이 표시 (보통 주간이 디폴트라는 사용자 피드백 반영)
- **자정 자동 전환**: 그동안 `currentDate`가 페이지 로드 시 한 번만 설정되고 자정이 지나도 갱신되지 않는 버그가 있었음(24시간 켜져 있는 키오스크라 심각한 문제였음) → `tickClock()`에 day-rollover 감지 추가. 화면이 "오늘"을 보고 있던 경우에만 자동으로 다음 날로 넘어가고, 관리자가 일부러 다른 날짜를 보고 있었다면 그대로 유지됨

### 3. 배포
- **GitHub Pages**: `ktdscon` 계정으로 `gh` CLI 설치·인증 후 `ktds_lobby` 공개 저장소 생성, Pages 활성화 → https://ktdscon.github.io/ktds_lobby/ 에서 정상 서비스 중
- **Google Apps Script**: 사용자가 직접 구글 시트 생성 + Apps Script 배포 (프로젝트명 "2026_lobby"), `?action=ping` 확인 완료

### 4. 웰컴 영상 기능 (VIP 방문 대응)
1년에 4~5회 정도 있는 손님 방문(산업인력공단 지사장, 사장님 등) 시 웰컴 영상을 띄우는 기능 추가.
- 관리자 모드 새 탭 "웰컴 영상": mp4 파일 업로드 + 시작/종료 일시 설정
- 영상 파일은 **IndexedDB**(브라우저 로컬 저장소)에 저장됨 — 구글시트/GitHub로는 안 보냄 (용량이 커서 부적합하고, 어차피 로비 PC 한 대에서만 재생되면 되므로 이 방식이 제일 간단함). 즉 **팀원 노트북에는 이 영상이 안 보임**, 로비 실물 PC 브라우저에만 업로드해야 함
- 설정한 시작~종료 시간 사이에는 전체화면 영상으로 자동 전환(1초마다 `tickClock`에서 체크), 지나면 자동으로 원래 화면 복귀
- **화면 터치 시 즉시 원래 화면으로 복귀** 가능 (그 시간 창이 끝날 때까지는 재발동 안 함 — `welcomeDismissedForWindow`로 추적)
- 관리자 톱니바퀴 버튼은 영상 재생 중에도 z-index로 위에 떠 있어서 항상 클릭 가능 (긴급 시 PIN으로 빠져나올 수 있음)
- 자동재생 사운드가 브라우저 정책으로 막히면 무음 재생 후 화면 우하단에 "🔇 소리 켜기" 버튼이 뜸
- **주의**: 처음 구현 시 `let welcomeVideoActive` 등의 상태 변수를 파일 하단(초기화 섹션 근처)에 선언했다가, `tickClock()`이 파일 상단에서 즉시 실행되면서 TDZ(temporal dead zone) 에러가 났음 — 상태 변수는 반드시 다른 `let` 변수들과 함께 상단에 선언할 것

### 5. 보안 사고 및 조치 (중요)
편의를 위해 실제 배포된 Apps Script URL과 `SHARED_KEY`(`ktds-lobby-2026`)를 index.html의 기본 설정값에 하드코딩했었는데, 이 저장소가 **공개(Public)**라서 그대로 노출되는 실수를 저질렀음.
- 즉시 되돌림: 기본값을 다시 빈 값으로 복원 (`apiUrl:'', apiKey:''`)
- `AppsScript_Code.gs`의 `SHARED_KEY` 예시값도 `CHANGE_ME_BEFORE_DEPLOY` 플레이스홀더로 변경
- 사용자에게 실제 Apps Script의 `SHARED_KEY`를 새 값으로 교체하고 같은 배포에 "새 버전"으로 재배포(URL 유지)하도록 안내함
- **주의**: git 커밋 히스토리에는 예전 키 값이 과거 커밋으로 남아있을 수 있음(rebase로 지우진 않음, 이미 무효화된 값이라 실익 없다고 판단) — 새 키를 SECRETS.local.md에만 적어두고, 절대 소스코드나 커밋 메시지에 다시 남기지 말 것
- **최종 정리**: 키 교체 과정에서 배포를 여러 번 새로 만들면서 Apps Script 웹앱 URL이 4개까지 생겼었음. 전부 확인 후 옛날 것 3개는 "배포 관리"에서 보관처리(Archive)해서 curl로 404(죽음) 확인 완료, 최종 URL 1개만 살아있는 상태로 정리함. 실제 URL/키는 SECRETS.local.md 참고

## 이번 세션에서 한 작업 (2026-09-12)

### 카카오톡 아침 알림 추가 (개인 일정용)

사용자 요청: "슬랙/옵시디언/캘린더에 알람을 걸어놔도 안 보는데, 카카오톡은 그나마 본다. 아침에 오늘 뭐가 있는지 카톡으로 알려달라. 예를 들면 다음 주 월요일에 사내 대부 대출 신청을 해야 하면 '대출 신청하세요'라고."

Claude는 세션이 끝나면 사라져서 매일 아침 스스로 카톡을 보낼 수 없다는 점을 먼저 짚고, **항상 켜져 있는 발송기**로 이미 쓰고 있는 구글시트 + Apps Script를 재활용했다.

- **KakaoReminder.gs** (신규): 카카오 "나에게 보내기" API로 매일 아침 브리핑 발송
  - 카카오 OAuth(인가코드 → refresh token)는 스크립트 속성에만 저장. 매일 발송하면서 refresh token이 자동 갱신되므로 사실상 만료 없음
  - 시트 `Reminders` 탭(날짜/할 일/시간/미리알림(일)/반복/완료/메모)이 데이터 소스. 폰의 구글 스프레드시트 앱에서 편집 가능
  - `미리알림(일)`에 `3,1`을 넣으면 D-3, D-1 아침에도 알려줌 → 사용자가 말한 "다음 주 월요일 대출 신청"이 정확히 이 케이스
  - 반복: `매일`/`평일`/`주말`/`매주 금`/`매주 월,수`/`매월 25`/`매월 말`/`매년 09-15` (한글 표기 파싱)
  - 카카오 text 템플릿 200자 제한 때문에 `chunkText_()`로 줄 단위 분할 후 `(1/2)` 표기로 나눠 전송
  - `setIncludeLobbySchedule(true)`를 켜면 그날 강의장 일정(`Schedule` 탭)도 메시지에 붙음
  - 보낼 내용이 없는 날은 발송을 건너뜀 (빈 알림으로 귀찮게 하지 않으려고)
- **AppsScript_Code.gs**: `doGet`에서 모르는 action을 `reminderDoGet_(e)`로 넘기는 3줄만 추가 (`typeof` 가드라 파일이 없어도 기존 동작 그대로). 폰에서 `?action=addReminder&...` / `?action=briefNow` 사용 가능
- **KAKAO_REMINDER_설정.md** (신규): 카카오 개발자 앱 생성 → 동의항목 → OAuth 1회 연결 → 트리거 설치까지 단계별 안내 + 시트 작성법 + 문제해결
- **index.html / _artifact_preview.html은 건드리지 않았음** — 로비 키오스크 화면과 완전히 무관한 기능이라 의도적으로 분리

검증: Apps Script 전역 객체(`SpreadsheetApp`/`Utilities`/`PropertiesService`/`Session`)를 스텁으로 바꾼 node 하니스로 반복 규칙·미리알림 파싱·날짜 정규화·200자 분할·브리핑 문구를 35개 케이스 통과시켰다. **카카오 실제 전송은 토큰이 있어야 하므로 미검증** — 사용자가 `sendTestKakao()`로 확인해야 함.

## 확인된 사항 / 남은 일

- 강의장 배치는 (리뉴얼) 최신 도면 + 실사 이미지 기준으로 정확히 맞춤 (201-212호, 사무실, 로비, 엘리베이터, 화장실, 입구 위치 포함)
- `[211] 28기 채용예정자과정`은 원본 데이터에 일요일 칸에만 찍혀 있어 평일 상시 진행으로 가정 처리함 — 정확한 요일 확인 필요
- **점검 완료 (2026-08-16)**:
  1. ✅ 실제 Apps Script `SHARED_KEY` 교체 확인 — 새 키로 실제 쓰기(POST) 테스트 성공, 테스트 데이터 정리 완료
  2. ✅ "설정 공유 링크" 생성 및 동작 확인 (자동저장+URL 파라미터 제거 확인됨, 링크는 SECRETS.local.md 참고)
  3. ✅ 8월 스케줄 데이터 86건 구글시트에 반영 완료 (2026-07-26~2026-09-05)
  4. ✅ 9월 스케줄 데이터 46건 반영 완료 (누적 118건, ~2026-10-08까지)
  5. ✅ 10~12월은 화면 테스트 확인용으로 "(테스트)" 표시된 임시 데이터만 넣어둠 (209호 주간/204호 야간, 매월 1일·15일) — **실제 데이터 아님, 나중에 진짜 데이터로 덮어써야 함**
  6. ⬜ 10~12월 실제 스케줄 데이터 입력 — 관리자 모드 붙여넣기 탭에서 월별로 반복 진행 필요 (테스트 데이터는 실제 데이터를 같은 날짜에 붙여넣으면 자동으로 덮어써짐)
  7. ⬜ 로비 PC에서 전체화면(F11)으로 실사용 테스트 — 원격으로는 확인 불가, 직접 필요
  8. ⬜ 웰컴 영상 기능을 실제 mp4 파일로 로비 PC에서 한 번 테스트 (지금까지는 가짜 Blob으로 로직만 검증함, 실제 영상 재생/자동재생 사운드 동작은 미검증)
  9. ⬜ 카카오톡 아침 알림 실제 연결 — KAKAO_REMINDER_설정.md 순서대로 카카오 개발자 앱 생성 → OAuth 1회 연결 → `sendTestKakao()`로 수신 확인 → `installDailyTrigger()`. 코드/로직은 검증됐지만 **실제 카카오 전송은 토큰이 있어야 해서 미검증** 상태

**저장/업데이트 메커니즘 자체는 전부 검증 완료됨**: 붙여넣기→자동반영, 개별 항목 수정/날짜이동, Apps Script 쓰기(POST) 전부 실제 배포 환경에서 동작 확인함. 남은 건 "기능"이 아니라 "실제 콘텐츠 입력"과 "현장 확인" 뿐임.
  9. 사용자가 2026-08-18 이후 다른 유료 Claude 계정으로 작업을 이어갈 예정 — 새 세션은 이 폴더를 열면 CLAUDE.md가 자동으로 로드되니 그대로 이어가면 되고, 막히면 이 문서를 먼저 읽을 것
